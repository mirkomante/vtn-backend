<%- include('../../../ui/detailViews/genericDetailView', {
  config: detailViewConfig,
  item: item,
  itemType: 'Menu Fisso',
  success: typeof success !== 'undefined' ? success : undefined,
  actionNavConfig: typeof actionNavConfig !== 'undefined' ? actionNavConfig : undefined
}) %>
