<% 
  const value = item[field.name];
  const symbol = field.format?.currency?.symbol || '€';
  const decimals = field.format?.currency?.decimals || 2;
%>
<% if (value !== null && value !== undefined) { %>
  <%= symbol %><%= parseFloat(value).toFixed(decimals) %>
<% } else { %>
  -
<% } %>
