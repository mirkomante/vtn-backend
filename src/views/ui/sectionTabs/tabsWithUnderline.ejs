<!-- 
  Componente Tabs con Underline
  Parametri dal layout:
  - sectionTabs: array di oggetti con { label, link, icon? }
  - currentPath: path corrente per determinare la tab attiva
-->
<div>
  <div class="grid grid-cols-1 sm:hidden">
    <!-- Select per mobile -->
    <select aria-label="Seleziona una sezione" class="col-start-1 row-start-1 w-full appearance-none rounded-md bg-white py-2 pr-8 pl-3 text-base text-gray-900 outline-1 -outline-offset-1 outline-gray-300 focus:outline-2 focus:-outline-offset-2 focus:outline-indigo-600">
      <% sectionTabs.forEach(tab => { %>
        <option value="<%= tab.link %>" <%= currentPath === tab.link ? 'selected' : '' %>>
          <%= tab.label %>
        </option>
      <% }); %>
    </select>
    <svg viewBox="0 0 16 16" fill="currentColor" data-slot="icon" aria-hidden="true" class="pointer-events-none col-start-1 row-start-1 mr-2 size-5 self-center justify-self-end fill-gray-500">
      <path d="M4.22 6.22a.75.75 0 0 1 1.06 0L8 8.94l2.72-2.72a.75.75 0 1 1 1.06 1.06l-3.25 3.25a.75.75 0 0 1-1.06 0L4.22 7.28a.75.75 0 0 1 0-1.06Z" clip-rule="evenodd" fill-rule="evenodd" />
    </svg>
  </div>

  <div class="hidden sm:block">
    <div class="border-b border-gray-200">
      <nav aria-label="Tabs" class="-mb-px flex space-x-8">
        <% sectionTabs.forEach(tab => { %>
          <% const isActive = currentPath === tab.link; %>
          <a href="<%= tab.link %>" 
             class="border-b-2 px-1 py-4 text-sm font-medium whitespace-nowrap <%= isActive ? 'border-indigo-500 text-indigo-600' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700' %>"
             <%= isActive ? 'aria-current="page"' : '' %>>
            <%= tab.label %>
          </a>
        <% }); %>
      </nav>
    </div>
  </div>
</div>
  